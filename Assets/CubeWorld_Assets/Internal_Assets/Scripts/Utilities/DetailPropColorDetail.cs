using AdvancedColorPicker;
using UnityEngine;

public class DetailPropColorDetail : MonoBehaviour
{
    public void SetupDetailPropColor()
    {
        //CustomizationManager.instance.SetupDetailPropColor(GetComponent<ColorPicker>().CurrentColor);
    }
}
